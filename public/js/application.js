import app from './components/app.js';
//import initWebsocket  from './lib/websocket.js';

// create a global event bus for socket io events
//Vue.prototype.$bus = new Vue({});

const vm = new Vue({
  el: '#app',
  
});

//initWebsocket(vm);

